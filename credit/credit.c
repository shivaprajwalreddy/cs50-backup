#include <cs50.h>
#include <stdio.h>

int main(void)
{
    long number;
    number = get_int("number: ");
}

int checksum(int number)
{
    // checksum for valid or invalid
}

int amex(int number)
{
    // 
}
